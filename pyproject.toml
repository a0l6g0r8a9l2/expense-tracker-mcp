[project]
name = "expense-tracker-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.2.2",
    "requests>=2.31.0",
    python-dotenv>=1.0.0",
]
